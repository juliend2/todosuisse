<%= form_with(model: task, local: true) do |form| %>
  <% if task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :task_description %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :task_status %>
  </div>

  <div class="field">
    <%= form.label :ordering %>
    <%= form.number_field :ordering, id: :task_ordering %>
  </div>

  <div class="field">
    <%= form.label :todo_on %>
    <%= form.date_select :todo_on, id: :task_todo_on %>
  </div>

  <div class="field">
    <%= form.label :done_at %>
    <%= form.datetime_select :done_at, id: :task_done_at %>
  </div>

  <div class="field">
    <%= form.label :task_template_id %>
    <%= form.text_field :task_template_id, id: :task_task_template_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
